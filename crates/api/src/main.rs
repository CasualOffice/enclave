//! `enclave-api` — HTTP surface, policy enforcement, MCP gateway
//!
//! Binary — composes the layers below it.
//!
//! See `docs/02-HLD.md §4` for where this crate sits in the architecture.

fn main() {
    // ENC-109: wiring lands with the policy engine.
    println!("enclave-api — not yet implemented");
}
