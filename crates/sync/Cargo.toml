[package]
name        = "enclave-sync"
description = "Delta cursors, device registry, sync eligibility"
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true
authors.workspace      = true

[dependencies]
enclave-core = { workspace = true }
enclave-db   = { workspace = true }
# For `READABLE_PREDICATE` — the one spelling of `CLAUDE.md` rule 9's SQL, spliced into the feed's
# version join rather than retyped. `crates/versions` exports it for exactly this
# (crates/versions/src/model.rs).
enclave-versions = { workspace = true }

anyhow      = { workspace = true }
chrono      = { workspace = true }
serde       = { workspace = true }
serde_json  = { workspace = true }
sqlx        = { workspace = true }
thiserror   = { workspace = true }
tracing     = { workspace = true }
uuid        = { workspace = true }

[dev-dependencies]
enclave-testing = { workspace = true }
tokio           = { workspace = true }

[lints]
workspace = true
